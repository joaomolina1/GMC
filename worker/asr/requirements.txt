# ASR do worker de clips — WhisperX (faster-whisper + alinhamento forçado) com pyannote (diarização).
# O whisperx fixa as versões compatíveis de torch/torchaudio/pyannote/ctranslate2 — não fixar aqui
# para evitar conflitos (ver worker/docs/runpod.md para o ajuste de LD_LIBRARY_PATH do cuDNN 9).
# Requer GPU NVIDIA com driver ≥ R525 (CUDA 12.x por compatibilidade de versão menor).
whisperx>=3.3.1
